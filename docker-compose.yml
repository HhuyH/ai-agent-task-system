services:
  ai-backend:
    build: ./ai-agent-backend
    container_name: ai-backend

    ports:
      - "8080:8080"

    environment:
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}